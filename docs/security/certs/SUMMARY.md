# docs/security/certs/

## Purpose
Sample TLS certificate and key files used for local HTTPS testing.

## How it works
- `localhost+2.pem` and `localhost+2-key.pem` are example PEM artifacts referenced by TLS setup instructions.
- These are intended for local development only, not production.
