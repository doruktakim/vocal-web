# tests/js/

## Purpose
Node-based sanity tests for extension security helpers.

## How it works
- `url-validator.test.js` checks protocol blocking, URL normalization, and domain allowlisting.
- `sensitive-fields.test.js` checks detection of passwords, OTPs, and credit-card metadata.
