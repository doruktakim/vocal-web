# extension/vendor/web-llm/

## Purpose
Stores the browser runtime asset consumed by the local WebLLM worker.

## Current state
- `index.js` is vendored from the official `@mlc-ai/web-llm` package (`0.2.80`).
- `LICENSE` is copied alongside the vendored runtime.
